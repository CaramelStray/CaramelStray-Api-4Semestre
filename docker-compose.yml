version: '3.8'
services:
  frontend:
    build: ./frontend
    container_name: tracker_frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/usr/src/app
      - /usr/src/app/node_modules